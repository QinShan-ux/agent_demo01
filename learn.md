#### 切片策略

##### 1.固定长度切片（有重叠）


##### 2.句子边界切片（无重叠）


##### 3.LLM语义切分


##### 4.层次切片
- 基于文档层次结构（标题 、章节、段落）进行切分。便于理解文档逻辑关系
- 保持文档结构

##### 5.滑动窗口切片

#### RAG

##### 高级召回

实现见 `rag/retrieval.py`，跑 `python -m rag.cli --mode all` 三种方式并排对比。

###### 1. BM25 关键词召回

- 补向量的短板：向量按语义把相近的排一起，但人名、条约名、专有术语这类字面精确匹配容易漏
- 公式：`score = Σ IDF(t) · tf·(k1+1) / (tf + k1·(1 − b + b·|d|/avgdl))`
  - `k1=1.5` 词频饱和：词在文中出现 10 次不该比 3 次强 3 倍
  - `b=0.75` 长度归一化：长文档天然更容易命中词，要打折
  - IDF 用 `ln(1 + x)` 形式，词出现在过半文档时仍为正，否则高频词会把命中变成负贡献
- 中文要分词，用 jieba；英文词统一小写
- 索引字段必须是**被向量化的那段文本**（子块），两路才在给同一份证据打分
- 倒排表稀疏打分（只算 query 里出现的词），406 条切片建表几十毫秒，不值得落盘缓存

###### 2. 混合检索 + RRF 融合

- 两路分数量纲不可比：余弦被归一化后挤在 0.3~0.7，BM25 无上界（实测能到 16）
- 加权融合要先 min-max 归一化，而归一化依赖候选池大小和 query，调参敏感
- RRF 只用排名：`score(d) = Σ 1/(k + rank_r(d))`，`k=60`
  - 天然免疫量纲，代价是丢掉分数幅度信息（所以子分数要打出来看）
  - 注意 RRF 分数**很小**：两路时理论上限 `2/61 ≈ 0.033`，别再套余弦的 0.6 阈值
- 去重：父子块模式下同一父块的多个子块会重复命中，按 `parent_idx` 用 **max** 聚合
  - 用 sum 会让子块多的大章节被系统性抬高（长度偏置）
  - 顺序必须是 融合 → rerank → 按父块 collapse，让交叉编码器来剪枝而不是 RRF 排名

###### 3. rerank 交叉编码器重排

- 双塔（embedding）是 query 和文档**分开**编码再算相似度，快但精度有限；交叉编码器把两者拼一起过一遍模型，准但慢——所以只用来重排前 20 条，不做全量召回
- 网关 `/rerank` 实测可用模型：`rerank-multilingual-v3.0`（区分度最好，0.897 / 0.000007）、`qwen3-rerank`、`rerank-v3.5`、`gte-rerank-v2`
- **送子块不送父块**：窗口约 512 token，约 1000 字符的父块会被截断，且截断点随机，真正命中的句子可能正好被切掉
- 返回的 `index` 只是本次请求 documents 数组里的下标，要按它对齐并防越界
- 失败时回退到融合排序，不抛异常：rerank 是增强项不是硬依赖

###### 实测效果（query：资本主义经济危机爆发的原因？）

| 名次 | vector | hybrid | hybrid + rerank |
|---|---|---|---|
| 1 | 专家点评 (0.7108) | 专家点评 (rrf 0.0325) | **相关链接 (0.9946)** |
| 2 | 相关链接 (0.6116) | 相关链接 (0.0318) | 专家点评 (0.9863) |

- 向量把「专家点评」排第一，但真正回答「直接原因」的是「相关链接」那段，rerank 把它顶到了第一
- 这是 rerank 最典型的收益：召回阶段排对了集合，但顺序不对，重排来纠正
